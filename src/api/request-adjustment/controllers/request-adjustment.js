'use strict';

/**
 * request-adjustment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::request-adjustment.request-adjustment');
