'use strict';

/**
 * process-step-notification router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::process-step-notification.process-step-notification');
