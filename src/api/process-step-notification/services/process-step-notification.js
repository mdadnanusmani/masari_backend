'use strict';

/**
 * process-step-notification service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::process-step-notification.process-step-notification');
