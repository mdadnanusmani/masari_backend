'use strict';

/**
 * survey-application router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::survey-application.survey-application');
