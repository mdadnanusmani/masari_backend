'use strict';

/**
 * department-document controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::department-document.department-document');
