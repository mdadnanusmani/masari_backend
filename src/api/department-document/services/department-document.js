'use strict';

/**
 * department-document service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::department-document.department-document');
